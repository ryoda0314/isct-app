# 医歯学時間割機能 進捗メモ (2026-04-11)

## 完了したこと

### Moodleデータ取得の仕組み
- 管理画面「Moodle取得」タブで対象ユーザーIDを指定 → ログイン時に自動キャプチャ
- 飯島さん(ID:10666)のデータ取得済み: 28科目（全て歯学部 DEN.A3xx）
- Moodleの `shortname` は理工系と同じ `DEN.A344[2026]` 形式
- `fullname` 内の `【021624】` が yushima2 シラバスの lct_cd

### シラバスデータ取得
- 管理画面「医歯学取得」タブ → yushima2.tmd.ac.jp から全セッションをスクレイプ
- `med_sessions` テーブルに日付・時刻・教室・教員をそのまま保存
- 歯学部(DEN): step=1で全コード列挙に修正済み、取得成功
- 医学部(MED): 取得済み（教養部含む）
- 大学院: **未修正** — コード範囲が間違っている（Too many requests エラー）

### 時間割表示
- `MedTTView` コンポーネント: 週間ビュー + カレンダービュー + 履修科目一覧
- DBから読む方式（スクレイプしない、即座に表示）
- 時間軸ベースの表示、時限ラベル付き
- 管理者アカウントではキャプチャデータからフォールバック

### 調査済み事項
- 021180(顎口腔医療) と 021181(顔面・顎・口腔疾患) は同一モジュール（時間100%一致）
  → 021181 を非表示にして 021180 を優先表示
- 全570科目調査: 時間完全重複は上記1組 + 選択コースの3組のみ
- モジュール分割パターン: 歯学部で15組確認（-A, -B, -C の枝番）

## 未対応

### 大学院コード範囲の修正
- `generateCandidateCodes` の default ケースが壊れている
- fac_cd: 31(修士), 36(博士3年), 41(博士4年), 48(JDP), 61(保健博士), 71(保健修士)
- 各大学院のコード範囲を実際に調査して設定する必要あり

### 学部学科設定モーダル（未実装）
- 未設定ユーザーに学部学科設定を促すモーダルを表示する
- 理工学系: マトリクス（既存の仕組み）
- 医歯学系: 学籍番号 → メール認証
- `user.myDept` が未設定の場合にトリガー
- `DEPTS` に医歯学系は登録済み（DEN_D, MED_M 等）
- `SCHOOLS` にも歯学部・医学部あり
- 設定すればサイドバーに学部・学科チャット/フィードが自動表示される

### ボトムバー切替（実装済み）
- 医歯学コースを持つユーザーはボトムバーの「時間割」が自動的に「医歯学時間割」に切り替わる
- 時間割・履修科目一覧からコースページ（タイムライン）への遷移も対応済み

### その他
- 大学院のunique index問題: 解決済み（indexを削除）
- MED/DEN/LIB/GRAD 全データ取得済み（計5,413セッション）
