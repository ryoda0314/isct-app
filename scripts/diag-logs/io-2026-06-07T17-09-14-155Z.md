# Disk IO 診断  2026-06-07T17:09:14.372Z
```
[
  {
    "v": "PostgreSQL 17.6 on aarch64-unknown-linux-gnu, compiled by gcc (GCC) 15.2.0, 64-bit",
    "db": "postgres"
  }
]
```

累積pg_stat_statements取得失敗: diag_sql: only read-only SELECT/WITH queries are allowed

## キャッシュヒット率（全テーブル合計）
```
{
  "__error": "diag_sql: only read-only SELECT/WITH queries are allowed"
}
```

## 物理読み込みが多いテーブル TOP25 (pg_statio_user_tables)
```
  (error: diag_sql: only read-only SELECT/WITH queries are allowed)
```

## seqスキャン/書き込みチャーン TOP25 (pg_stat_user_tables)
```
  (error: diag_sql: only read-only SELECT/WITH queries are allowed)
```

## テーブルサイズ TOP25
```
  (error: diag_sql: only read-only SELECT/WITH queries are allowed)
```

## 未使用インデックス（idx_scan=0, IO書き込みの無駄）
```
  (error: diag_sql: only read-only SELECT/WITH queries are allowed)
```

## 現在のアクティブクエリ
```
  (error: diag_sql: only read-only SELECT/WITH queries are allowed)
```